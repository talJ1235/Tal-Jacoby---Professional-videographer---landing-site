import './Works.css';

// Placeholder — works grid, filters, previews and player are built in later steps.
export function Works() {
  return <section id="works" className="works" aria-label="עבודות" />;
}
