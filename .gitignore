# Dependencies
node_modules/

# Build output
dist/
.vercel/

# Environment — לעולם לא להעלות secrets אמיתיים
.env
.env.local
.env.production
.env.*.local

# Database
*.db
*.db-journal

# Claude Code workspace — skills + launch config are COMMITTED so they travel
# with the repo (multi-account handoff). Only the per-machine local settings and
# ad-hoc scratch are ignored.
.claude/settings.local.json
.claude/*.local.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# IDE
.vscode/settings.json

# קבצי תמונה זמניים בשורש
*.png.png
favicon.png.png
.env.vercel
